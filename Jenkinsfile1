pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the application...'
                // Example build command
                //sh './build.sh'

go mod init wordsmith
                    go mod tidy
                    go build
                ...
            }
        }
    }
}
